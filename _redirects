# ============================================================
# Minpaku Resort — Netlify リダイレクト設定
# ============================================================

# 存在しないパスは 404 を返す（静的サイト構成）
# ※ SPA用の /* /index.html 200 は削除済み（都市ページが静的HTMLのため不要）
